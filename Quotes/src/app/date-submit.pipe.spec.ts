import { DateSubmitPipe } from './date-submit.pipe';

describe('DateSubmitPipe', () => {
  it('create an instance', () => {
    const pipe = new DateSubmitPipe();
    expect(pipe).toBeTruthy();
  });
});
